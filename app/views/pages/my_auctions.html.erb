<div class="container">
  <div class="row">
    <% @artworks.each do |art| %>
      <div class="col-4">
        <strong><%= art.name %></strong>
        <% art.bids.where(user: current_user).order(:price).each do |bid| %>
          <em><%= "#{bid.created_at}: #{bid.price}" %></em>
          <em><%= "#{bid.created_at}: #{bid.price}" %></em>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
