<div class="container">
  <% @artworks.each do |art| %>
    <div class="card-product">
      <%= link_to art_path(art) do %>
         <%= cl_image_tag art.images[0].key %>
      <% end %>
      <div class="card-product-infos">
        <h2><%= art.name %></h2>
        <p>Your current bid: £ <%= art.bids.where(user: current_user).order(:price).last.price %></p>
        <p> <strong>Location:</strong> <%= art.location %>
          <%# <em><%= "#{bid.created_at}: #{bid.price}" %>
          <%# <em><%= "#{bid.created_at}: #{bid.price}" %>
      </div>
    </div>
  <% end %>
</div>
