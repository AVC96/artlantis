<div class="banner d-flex justify-content-center flex-column">
  <div class="container justify-content-center d-flex flex-column text-center">
    <div class="title_box">
      <h1>Artlantis | Digital Art Platform</h1>
    </div>
    <div class="art-container">
      <div class="row">
        <% @bids.each do |bid| %>
          <div class="col-4">
            <div class="m-3">
              <%= link_to art_path(art) do %>
                <%= cl_image_tag art.images[0].key, class:'w-100 p-3 bg-light'%>
              <% end %>
              <div class="art_text">
                <span><%= art.name%></span>
                <p> My Bid Amount £<%= @my_auctions.cost%></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
